import { WorkingModeLabelPipe } from './working-mode.pipe';

describe('WorkingModeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkingModeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
