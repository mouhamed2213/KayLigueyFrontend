import { ContractLabelPipe } from './contract-label.pipe';

describe('ContractLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
